# Rust build artifacts (Cargo.lock is also ignored repository-wide)
server/target/
server/Cargo.lock

# temporary symlink created by proto/protocgen.sh during `make proto-gen`
proto/ibc

# generated Go output before protocgen.sh moves it to client/pb
github.com/

# vendored proto import closure, regenerated by `make proto-gen`
proto/third_party/
